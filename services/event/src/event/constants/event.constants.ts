export enum EventStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum EventConditionType {
  LOGIN_STREAK = 'LOGIN_STREAK',
  FRIEND_INVITATION_COUNT = 'FRIEND_INVITATION_COUNT',
  LEVER_GOAL = 'LEVER_GOAL',
}

export enum EventRewardType {
  POINT = 'POINT',
  ITEM = 'ITEM',
  COUPON = 'COUPON',
}
